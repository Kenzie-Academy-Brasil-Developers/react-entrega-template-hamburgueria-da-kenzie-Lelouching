import styled from "styled-components"

export const CartStyle = styled.ul`
    
    width: 100%;
    height: fit-content;
    max-height: 22.5rem;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    border-radius: 0px 0px var(--radius-2) var(--radius-2);
    padding: 1rem 0.5rem;
    background-color: var(--color-grey-4);
` 